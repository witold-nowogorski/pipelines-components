# Autorag

This subcategory contains components in the **Autorag** group:

- [Prepare Responses API Requests](./build_responses_request_bodies/README.md): Emit one Llama Stack ``POST /v1/responses`` JSON body per RAG pattern directory.
